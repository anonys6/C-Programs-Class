#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    char a[100], b[100], c[100];
    gets(a);
    gets(b);
    gets(c);
    printf("%s", a);
    printf("%s", b);
    printf("%s", c);
    return 0;
}