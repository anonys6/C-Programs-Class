#include <stdio.h>

void main()
{
    // int age;

    // printf("Enter your age: ");
    // scanf("%d", &age);
    // (age >= 18) ? (printf("Eligible\n")) : (printf("not Eligible\n"));

    // int x, y, max;
    // printf("Enter two numbers");
    // scanf("%d %d", &x, &y);
    // max = x > y? x : y;
    // printf("Greater num is %d", x > y? x : y);
    clrscr();

}.;