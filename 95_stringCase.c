#include <stdio.h>
#include <string.h>

int main() {
    // char s[] = "hello";
    // strupr(s);
    // puts(s);
    // return 0;
    // s = "hell";
    // upper lower compare concat
    char s[] = "hello";
    
    // char a[strlen(s)];

    strupr(s);

    printf("%s", s);

    return 0;
}